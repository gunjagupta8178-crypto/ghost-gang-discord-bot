[project]
name = "ghost-gang-discord-bot"
version = "0.1.0"
description = "A simple Ghost Empire welcome bot built with discord.py."
requires-python = ">=3.11"
dependencies = [
    "discord-py>=2.7.1",
]
